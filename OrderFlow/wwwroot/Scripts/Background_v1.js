function getHeightInPx(percentage) {
    var height = window.innerHeight;
    return (height * percentage) / 100;
}
function getWidthInPx(percentage) {
    var width = window.innerWidth;
    return (width * percentage) / 100;
}